/*package org.academiadecodigo.asynctomatics.sokovando;

public class Box{
    private Position pos;

    public Box(Position pos){
        this.pos = pos;
    }

    public boolean receiveMove(Direction direction){
        if (!checkEdges.box(pos.move(direction)) && (!checkEdges.wall(pos.move(direction)))) {
            pos.move(direction);
            return true;
        }
        return false;
    }
}
*/